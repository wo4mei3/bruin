fn func<T>(e:T) -> T {
    let ptr:fn (int) -> int = fn func<int>;
}

struct Obj<T> {
   field:T;
}

enum Option<T> {
  Some(T);
  None;
}


impl Obj {
    fn (Self*) method<T>(x:int, arr:int[]) -> T  {
        let ptr:int* = 0;
        while a> b {
        }
        for i in l {
        }
        switch e {
            case _ : {
                
            }
        }
        
    }
}
