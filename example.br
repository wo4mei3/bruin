
struct Obj {
   field:T;
}

enum Option<T> {
  Some(T);
  None;
}


impl Obj {
    fn (Self*) method<T>(x:int, arr:int[]) -> fn (T) -> fn(T) -> T  {
        let ptr:int* = 0;
        while a> b {
        }
        for i in l {
        }
        switch e {
            case _ : {
                
            }
        }
        
    }
}
