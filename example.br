fn func<T,U>(e:Obj<T>) -> Obj<U> {
    let ptr:int = 0;
}

struct Obj<T> {
   field:T;
}

enum Option<T> {
  Some(T);
  None;
}


impl Obj {
    fn (Self*) method<T>(x:int, arr:int[]) -> T  {
        let ptr:int* = 0;
        while a> b {
        }
        for i in l {
        }
        switch e {
            case _ : {
                
            }
        }
        
    }
}
