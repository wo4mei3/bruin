fn func(e:int) -> int {
    let ptr:fn (int) -> int = func;
}

struct X {}

struct Obj<T> {
   field:T;
}

enum Option<T> {
  Some(T);
  None;
}


impl Obj {
    fn (Self*) method<T>(x:int, arr:int[]) -> T  {
        let ptr:int* = 0;
        while a> b {
        }
        for i in l {
        }
        switch e {
            case _ : {
                
            }
        }
        
    }
}
