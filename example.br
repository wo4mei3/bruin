
struct Obj {
   field:T1;
}

enum Option<T> {
  Some(T);
  None;
}


impl Obj {
    fn (Self*) method<T>(x:int, arr:int[]) -> T  {
        let ptr:int* = 0;
        while b {
        }
        for i in l {
        }
        switch e {
            case _ : {
                
            }
        }
        
    }
}
